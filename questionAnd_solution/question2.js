// Write an if-else statement that checks if a variable y is equal to 0. If it is, log "y is zero" to the console. Otherwise, log "y is not zero".

let y=0

if(y==0){
     console.log("y is zero")
}
else{
    console.log("y is not Zero")
}