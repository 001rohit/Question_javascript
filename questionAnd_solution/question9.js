// Write an if-else statement that checks if a variable f is a boolean. If it is, log "f is a boolean" to the console. Otherwise, log "f is not a boolean".

let f = true
// console.log(typeof(f))
if(f===true || f===false){
    console.log("f is boolean")
}
else{
    console.log("f is not boolean")
}