// Write an if-else statement that checks if a variable c is a string. If it is, log "c is a string" to the console. Otherwise, log "c is not a string".

let c ="hellow"

if(c === c.toString()){
    console.log("c is string")
}
else {
    console.log("c is not string")
}