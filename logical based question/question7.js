// Write a JavaScript program to check if a number is positive, negative, or zero using logical operators.

let num=15
let res;
if(num<0){
    res = `${num} is negatvie number`
}
else if(num==0){
    res = `${num} is zero`
}
else{
    res =`${num} is postive`
}
console.log(res);
