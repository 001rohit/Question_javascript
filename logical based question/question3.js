// Write a JavaScript program to check if a number is divisible by 3 and 5.

let num = 60
if(num%3==0 && num%5==0){
    res = `${num} number is divisible by 3 and 5 `
}
else{
    res = `${num} number is not divisible by 3 and 5 `
}
console.log(res);